name: Solana Monitor CI
on: [push, pull_request]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Environment
        uses: actions/setup-python@v4 # หรือภาษาที่บอสใช้ในโปรเจกต์นี้
        with:
          python-version: '3.10'

      - name: Syntax Check (catch smart quotes / bad indentation early)
        run: |
          find . -name "*.py" -print0 | xargs -0 -n1 python -m py_compile

      - name: Run Coverage Test
        run: |
          pip install pytest pytest-cov
          pytest --cov=./ --cov-report=xml

      - name: Upload to Codecov
        uses: codecov/codecov-action@v5
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          slug: scutuatua-crypto/solana-monitor
